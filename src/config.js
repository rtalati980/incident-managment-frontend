
// config.js
const config = {
    API_BASE_URL: 'https://incident-managment-backend.onrender.com' || 'http://localhost:5000' || 'https://incident-managment-backend.onrender.com',
  };
  
  export default config;
  
